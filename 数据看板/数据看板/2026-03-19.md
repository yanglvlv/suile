# 2026-03-19

## 结算系统 - 看板指标数据面板
- `index.html` 重构为纯数据展示面板（去掉导入模态框，导入功能已有现成的）
- 左侧导航栏 + 右侧数据表格，标准表头共 51 个字段
- **搜索筛选区域**：表格上方加 12 个筛选字段（订单创建月份、系统来源、产品名称、平台、验收状态、采买类型、场景、合作模式、账号名称、订单号、账号分级、数据版本），支持"更多筛选"展开收起
- 筛选组件：下拉选择（自动提取选项）+ 文本输入模糊搜索 + 月份选择器
- 已选筛选条件以标签形式展示，支持单个清除
- 工具栏：数据统计摘要 + 全局搜索 + 导出本页/全部 + 分页选择
- 数据表格支持：排序、分页、数字千分位格式化、验收状态/账号分级颜色标签
- 左侧导航「薪酬导入」保留为独立菜单项（对应已有的导入功能）

## 电子签发起相关 - 变更前后流程对比
- 基于 `电子签发起相关-青梅系统-TAPD平台.pdf`（TAPD ID: 131145775）制作流程对比页面
- 输出到 `电子签/index.html`
- 改为**可视化流程图**形式的变更前后左右对比（用户要求不是字段对照表，而是完整流程对比）
- 4 组流程对比：① 推送电子签核心操作流程（5步→8步）② 合同本地状态流转 ③ 合同管理页面结构 ④ 推送操作交互
- 每个步骤标注角色（操作人/Leader/系统）+ 变更标签（无变化/变更/新增）
- 核心变更：推送电子签增加 Leader 审核环节、新增经办人概念、新增审核页面、合同状态扩展（推送审核/推送驳回）

## 表头字段检测方案设计文档
- 输出了 `表头字段检测方案设计.md`，目前更新到 v3.0
- 用户去掉了别名字典和位置参考法，精简为 3 级匹配策略
- 最终方案：精确匹配（含格式标准化）→ 关键词拆解（Jaccard + 区分性关键词约束）→ 字符串相似度（Levenshtein + Jaro-Winkler）
- 加上匈牙利算法做全局最优一对一映射 + 用户手动干预
- v3.0 重构为 7 步流程方案（文件解析→格式标准化→三级匹配→全局分配→用户确认→数据重映射→导出），包含数据结构定义、异常场景处理和技术选型
